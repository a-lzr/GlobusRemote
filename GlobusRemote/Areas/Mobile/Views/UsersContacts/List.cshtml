@model List<GlobusRemote.Areas.Mobile.Models.UsersContactsListViewModel>
@using Localize = GlobusRemote.Localize.Index

@{
    ViewData["Title"] = @Localize.MobilePage_UsersContacts_Title;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="divTable">
    <div class="divTableBody">
        <div class="divTableHeadRow">
            <div>
                @Localize.Field_Base_Id
            </div>
            <div>
                @Localize.Field_Base_ParentLink
            </div>
            <div>
                @Localize.Field_Base_Name
            </div>
            <div>
                @Localize.Field_Base_Phone
            </div>
            <div>
                @Localize.Field_Base_FlagExpire
            </div>
            <div>
                @Localize.Field_Base_DateCreated
            </div>
            <div>
                @Localize.Field_Base_DateChanged
            </div>
            <div>
                @Localize.Field_Base_Operations
            </div>
        </div>
        @foreach (var item in Model)
        {
            <div class="divTableRow">
                <div>
                    @item.Fid
                </div>
                <div>
                    @item.FparentLink
                </div>
                <div>
                    @item.Fname
                </div>
                <div>
                    @item.Fphone
                </div>
                <div>
                    @item.FflagExpire
                </div>
                <div>
                    @item.FdateCreated
                </div>
                <div>
                    @item.FdateChanged
                </div>
                <div>
                    @if (@item.CanEdit)
                    {
                        <form action="/Mobile/UsersContacts/Remove?id=@item.Fid" method="post">
                            <input type="submit" title="Удалить текущую запись" value="Удалить" onclick="return confirm('Вы действительно хотите удалить выбранную запись?')">
                        </form>
                    }
                </div>
            </div>
        }
    </div>
</div>